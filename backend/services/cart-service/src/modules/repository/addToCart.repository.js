import initializeModels from "../../models/index.js";

const db=await initializeModels();
const {Cart,CartItem}=db;


async function addToCart(userId,productId,variantId,quantity,priceSnapshot){
    try {
        
    } catch (error) {
        
    }
}

